<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.codersof.mercaditoapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar"
        tools:ignore="AllowBackup">
        <activity android:name=".bashboardActivity" />
        <activity android:name=".cambiarcontrasenaactivity" />
        <activity android:name=".registraractivity" />
        <activity android:name=".loginActivity" />
        <activity android:name=".tour_activity_tres" />
        <activity android:name=".TourActivityDos" />
        <activity android:name=".TourActivityUno" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>